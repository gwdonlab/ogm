name: Pylint
on: [push]
jobs:
  Pylint:
    runs-on: ubuntu-latest
    steps:
      - run: pip install pylint 
      - run: pip install cython
      - run: ls
      - run: pip install -r ogm/requirements.txt
      - run: pylint ogm
